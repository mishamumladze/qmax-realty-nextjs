========================================================
LINT FIX EVIDENCE — ThemeToggle.tsx
Date: 2026-08-19
Task: Fix react-hooks/set-state-in-effect + @typescript-eslint/no-unused-vars
========================================================

1. FILE CHANGE SUMMARY
--------------------------------------------------------
File: src/components/ThemeToggle.tsx

BEFORE (35 lines):
  - Used useState(false) + useEffect(() => { setMounted(true); }, [])
    → Triggers react-hooks/set-state-in-effect (setState in effect body)
  - Destructured { theme, resolvedTheme, setTheme } from useTheme()
    → `theme` was unused, triggering @typescript-eslint/no-unused-vars

AFTER (36 lines):
  - Replaced useState + useEffect with useSyncExternalStore hydration pattern:
      const mounted = useSyncExternalStore(emptySubscribe, () => true, () => false)
    → Returns true on client, false on server. Zero effects, zero setState.
  - Destructured { resolvedTheme, setTheme } from useTheme()
    → Removed unused `theme` variable.

Diff: Replaced entire file (imports, state mechanism, destructuring).
      No eslint-disable, no @ts-ignore, no @ts-expect-error, no `as any`.

2. LINT OUTPUT (npm run lint)
--------------------------------------------------------
Command: npm run lint
Exit code: 0
Timeout enforced: 120s (completed in ~2s)

Output:
  > qmax-realty@0.1.0 lint
  > eslint

  (no errors, no warnings)

3. TYPE CHECK OUTPUT (npx tsc --noEmit)
--------------------------------------------------------
Command: npx tsc --noEmit
Exit code: 0
Timeout enforced: 120s (completed in ~5s)

Output:
  (no errors)

4. BROWSER SMOKE TEST (Playwright)
--------------------------------------------------------
Dev server: Existing PID 1264 on port 3000 (hot-reloaded after file edit)
URL: http://localhost:3000
Timeout enforced: 90s for dev-ready (served immediately from existing server)

Assertions:
  [PASS] Toggle button visible: [aria-label="Toggle theme"] found at ref=e17
  [PASS] Initial html class: "light"
  [PASS] After click 1 — html class: "dark"
  [PASS] After click 1 — icon class: "lucide lucide-sun w-5 h-5 text-amber-400"
  [PASS] After click 2 — html class: "light"
  [PASS] After click 2 — icon class: "lucide lucide-moon w-5 h-5 text-gray-700"
  [PASS] Zero console errors (total: 3 messages, errors: 0, warnings: 0)

Screenshot: .omo/evidence/qa-lint-fix-smoke.png

5. CLEANUP
--------------------------------------------------------
Dev server: Existing server (PID 1264) was NOT killed — it was pre-existing
before this task and is still needed for other development.
Background attempt to start new dev server (PID 23224) failed (port conflict),
exited cleanly. Port 3000 remains occupied by PID 1264 as expected.

Cleanup receipt: No new processes to clean up. Existing dev server preserved.

6. VERIFICATION ORDER (adversarial notes)
--------------------------------------------------------
- File write performed BEFORE lint/tsc/smoke → no stale state verified
- Lint and tsc ran AFTER file write → fresh build verified
- Browser test loaded page AFTER file write + dev hot-reload → current code verified
- All commands had explicit timeouts enforced
- All assertions print observed values (see section 4)
