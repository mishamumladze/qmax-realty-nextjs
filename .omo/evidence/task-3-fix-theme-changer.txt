TASK 3 EVIDENCE: Fix theme changer to use resolvedTheme
File: src/components/ThemeToggle.tsx
Date: 2026-08-19

BEFORE/AFTER DIFF
-----------------
Line 8:
- BEFORE: const { theme, setTheme } = useTheme();
+ AFTER:  const { theme, resolvedTheme, setTheme } = useTheme();

Line 20:
- BEFORE: const isDark = theme === "dark";
+ AFTER:  const isDark = resolvedTheme === "dark";

All other content unchanged (mounted guard, placeholder div, onClick handler,
classNames, imports all preserved).

RATIONALE
---------
With defaultTheme="system", the stored `theme` value is "system", so comparing
`theme === "dark"` would make the icon disagree with the actually-applied theme.
`resolvedTheme` is the applied theme (always "light" or "dark").

VERIFICATION
------------
1. File read-back: confirmed `resolvedTheme` is destructured on line 8 and used
   in the isDark line (line 20).
2. Search for `theme === "dark"` in the file: NO MATCHES (grep returned
   "No matches found").

ACCEPTANCE CRITERION
--------------------
PASS - resolvedTheme is destructured and used for isDark; no occurrence of
`theme === "dark"` remains in the file; all other content unchanged.