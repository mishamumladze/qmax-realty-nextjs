TASK 2: Fix theme changer (ThemeToggle in layout.tsx)
======================================================

BEFORE
------
// src/app/layout.tsx
import { ThemeProvider } from "@/components/ThemeProvider";
import Footer from "@/components/Footer";
import "@/app/globals.css";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="flex flex-col min-h-screen">
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          <main className="flex-grow">{children}</main>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}

AFTER
-----
// src/app/layout.tsx
import { ThemeProvider } from "@/components/ThemeProvider";
import Footer from "@/components/Footer";
import ThemeToggle from "@/components/ThemeToggle";
import "@/app/globals.css";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className="flex flex-col min-h-screen">
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          <main className="flex-grow">{children}</main>
          <div className="fixed top-4 right-4 z-50"><ThemeToggle /></div>
          <Footer />
        </ThemeProvider>
      </body>
    </html>
  );
}

DIFF
----
+ import ThemeToggle from "@/components/ThemeToggle";
+ <div className="fixed top-4 right-4 z-50"><ThemeToggle /></div>

VERIFICATION
------------
- File read back after edit: confirmed.
- Import line `import ThemeToggle from "@/components/ThemeToggle";` present (line 4).
- Toggle rendered inside <ThemeProvider> JSX tree, between <main> and <Footer />, with className "fixed top-4 right-4 z-50" (line 17).
- ThemeProvider props (attribute="class" defaultTheme="system" enableSystem), html lang/suppressHydrationWarning, body className, main element, and Footer all unchanged.
- No other files edited. No npm install run. No build/dev server run.

PASS: ThemeToggle imported and rendered inside ThemeProvider with className "fixed top-4 right-4 z-50".